{
  "name": "estimate-schema",
  "version": "0.0.1",
  "description": "a cli utility for generating data schema estimates with a bunch of input data, useful for zod or other schema validation library.",
  "main": "index.cjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "3dwardsharp",
  "license": "GPL-3.0-or-later"
}
